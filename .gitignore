tags
test.sh
.luarc.json
nvim

spell/

# NOTE: `nvim-pack-lock.json` is deliberately NOT ignored here - it is tracked in
# version control so plugin versions are reproducible. See `:help vim.pack-lockfile`.
# Upstream `nvim-lua/kickstart.nvim` ignores it to avoid unneeded merge conflicts.
# nvim-pack-lock.json

.DS_Store
